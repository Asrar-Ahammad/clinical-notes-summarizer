{
  "name": "clinical-notes-summarizer-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist node_modules",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/aws-lambda": "^8.10.92"
  }
}
